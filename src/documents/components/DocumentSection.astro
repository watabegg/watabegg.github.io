---
type Props = {
	title: string
	compact?: boolean
}

const { title, compact = false } = Astro.props
---

<section class:list={['document-section', { 'document-section--compact': compact }] }>
	<h2>{title}</h2>
	<div class="document-section-content">
		<slot />
	</div>
</section>
